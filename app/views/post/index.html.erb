<h1>Post#index</h1>
<p>Find me in app/views/post/index.html.erb</p>

<h1> Posts </h1>
<% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <h2><%= post.title %></h2>
  <p><%= post.title %></p>
  <%= link_to 'Show post', post_path(post.id)%>
<% end %>